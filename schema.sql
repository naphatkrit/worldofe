CREATE TABLE data (
       owner VARCHAR(255), 
       url VARCHAR(255),
       labels TEXT,
       data TEXT,
       layout VARCHAR(255),
       ip VARCHAR(255),
       headers TEXT,
       created INT
);